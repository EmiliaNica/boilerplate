export class studentRecords{
    constructor(public course:string, public teacher:string, public grade:number){

        this.course = course;
        this.teacher = teacher;
        this.grade = grade;

    }
}